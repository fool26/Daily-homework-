#include<stdio.h>
#include<math.h>
int main(void)
{
    int a,b,c,i,s=0,p=0;
    scanf("%d %d %d",&a,&b,&c);
    for(;a<=b;a++)
    {
        for(i=1;i<a;i++)
        {
            if(a%i==0)
                s=s+i;
        }
        if(a!=1)
        {if((fabs(a-s))<=c)
            {printf("%d",a);
             p++;
             if(p%5==0)
                printf("\n");
            else
                printf("\t");
            }
        i=1;
        s=0;
    }}
    if(p%5!=0)
        printf("\n");
    if(p==0)
        printf("There is no proper number in the interval.\n");
        return 0;
}
