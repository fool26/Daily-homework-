#include<stdio.h>
int main(void)
{
	int i,j,ar[100],temp,n,ele;
	scanf("%d",&n);//输入数组中元素总个数（<100） 
	for(i=0;i<n;i++)
	{
		scanf("%d",&ele);//扫入数组元素 
		ar[i]=ele;
	}
	for(i=0;i<n-1;i++)//第i+1个元素与后面的元素一一比较，若后面数小则替换 
	{
		for(j=i+1;j<n;j++)
		{
			if(ar[j]<ar[i])
			{
				temp=ar[i];
				ar[i]=ar[j];
				ar[j]=temp;
				
			}
		}
	}
	for(i=0;i<n;i++)//扫出数组 
	{
		printf("%d ",ar[i]);
	}
	return 0;
} 
