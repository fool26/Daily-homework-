#include<stdio.h>
int main()
{
    int n,i,m,num2=0,num1=0,min;
    scanf("%d %d",&n,&m);
    if(n>m)min=m;
    else min=n;
    for(i=0;min>=i;i++)
    {
        num1=num1+(n-i)*(m-i);
    }
    num2=((n+1)*n*(m+1)*m)/4;
    printf("%d %d",num1,num2-num1);
    return 0;
}
