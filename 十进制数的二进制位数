#include<stdio.h>
int countBinary(int n);
int main(void)
{
	int n;
	scanf("%d",&n);
	printf("%d",countBinary(n));
	
	return 0;
}
int countBinary(int n)
{
	if(n==0||n==1)
	return 1;
	else
	return 1+countBinary(n/2);
}
